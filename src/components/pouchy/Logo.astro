---
/** Root-brand logo (parked, not published): the bare Pouchy wordmark. */
import PouchyWordmark from '../PouchyWordmark.astro';

interface Props {
  /** Tailwind classes for the logo (height and color) */
  class?: string;
}

const { class: className = 'h-7 text-ink' } = Astro.props;
---

<PouchyWordmark class={className} />
