---
import { getLang, homeHref } from '../i18n';

interface Props {
  title?: string;
  description?: string;
}

const lang = getLang(Astro.currentLocale);

const defaults = {
  sk: {
    title: 'Pauči | Jednoduchá regulácia mobilov v školách',
    description:
      'Pauči je uzamykateľné puzdro na mobil určené na jednoduchú reguláciu používania telefónov počas vyučovania.',
  },
  en: {
    title: 'Pauči | Simple phone regulation in schools',
    description:
      'Pauči is a lockable phone pouch designed for simple regulation of phone use during lessons.',
  },
} as const;

const {
  title = defaults[lang].title,
  description = defaults[lang].description,
} = Astro.props;

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <link rel="alternate" hreflang="sk" href={new URL(homeHref('sk'), Astro.site)} />
    <link rel="alternate" hreflang="en" href={new URL(homeHref('en'), Astro.site)} />
  </head>
  <body class="antialiased text-ink bg-cream overflow-x-clip">
    <slot />
  </body>
</html>
